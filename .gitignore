# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/

# Claude Code
.claude/

# Áudios gerados localmente pelo scripts/gerar-audios.mjs (ficam no Supabase Storage)
scripts/audios-gerados/
# Leituras (audiolivro) geradas por scripts/gerar-leituras.mjs (ficam no Supabase Storage)
scripts/leituras-geradas/
scripts/pastor-imperfeito-gemini.log
scripts/pastor-imperfeito-gemini-launchd.log
# venv do edge-tts (motor de TTS de leitura, grátis); recriar com python3 -m venv
scripts/.venv-tts/

# Supabase CLI
supabase/.temp

# Artefatos de trabalho que não devem ser versionados.
# ATENÇÃO: tmp/livros/ É versionado de propósito (73 json de importação de
# livro) — por isso o ignore é por caminho, não a pasta tmp/ inteira.
tmp/pdfs/
tmp/english-audio.sql

# Python (scripts de extração)
__pycache__/
*.pyc
